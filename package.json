{
  "name": "freestyle-app",
  "version": "1.0.0",
  "description": "Freestyle App with React Native frontend and Node.js backend",
  "scripts": {
    "start": "cd server && npm start",
    "install": "cd server && npm install",
    "build": "cd server && npm run build",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
